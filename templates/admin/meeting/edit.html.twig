{% extends 'base.html.twig' %}

{% block title %}Izmeni sastanak - Admin{% endblock %}

{% block body %}
    <div class="whiteCard50">
        <h1>Izmeni sastanak</h1>
        <br>
        {%  if error_msg %}
            <div class="alert alert-danger">
                {{ error_msg }}
            </div>
        {% endif %}
        {{ include('admin/_form.html.twig',{
            btn_title: 'Izmeni'
        }) }}
    </div>
{% endblock %}